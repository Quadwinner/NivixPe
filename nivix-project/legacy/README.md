# legacy/

Parked proof-of-concept files that are **no longer wired into anything** but kept for reference during migration. Do not build on these.

- `automated-transfer-server.js` — old standalone auto-transfer server (not referenced by any script).

The larger legacy POC still lives at its original paths during migration (`bridge-service/`, `frontend/nivix-pay-old/`) because the operational scripts are coupled to those locations — see [`../docs/PROJECT_STRUCTURE.md`](../docs/PROJECT_STRUCTURE.md).
